package main;

//TCPClient.java


class TCPClient {
	static Chat chatterini;
	static Thread chatter;
	public TCPClient(String readLine) {
		chatterini = new Chat(String readLine);
		chatter = new Thread(chatterini);
		chatter.start();
	}
}